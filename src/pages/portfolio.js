import * as React from 'react'
import Layout from '../components/layout'

const AboutPage = () => {
    return (
        <Layout pageTitle="Portfolio Page">
            <p>This is the portfolio page</p>
        </Layout>
    )
}

export const Head = () => <title>Web Developer - about</title>

export default AboutPage